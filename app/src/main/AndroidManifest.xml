<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bohui">

    <permission
        android:name="${applicationId}.permission.JPUSH_MESSAGE"
        android:protectionLevel="signature" />
    <uses-permission android:name="${applicationId}.permission.JPUSH_MESSAGE" />
    <!-- 网络相关 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--用于访问GPS定位-->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"></uses-permission>
    <!--用于获取wifi的获取权限，wifi信息会用来进行网络定位-->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"></uses-permission>
    <!--用于申请调用A-GPS模块-->
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS"></uses-permission>
    <!--未知来源安装-->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!--允许程序设置内置sd卡的写权限-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!--允许程序访问WiFi网络信息-->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!--允许程序读写手机状态和身份-->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!--允许程序访问CellID或WiFi热点来获取粗略的位置-->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!--sdcard读写权限-->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:name=".BoHuiApplication"
        android:allowBackup="true"
        android:appComponentFactory="appComponentFactory"
        android:configChanges="locale"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning"
        tools:replace="android:appComponentFactory,icon">
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.BaseActivity"
            android:theme="@style/SmartMarketTheme.NoTitle" />
        <activity
            android:name=".activity.FixedOnTopToolbarActivity"
            android:theme="@style/SmartMarketTheme.NoTitle" />
        <activity
            android:name=".activity.MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SmartMarketTheme.NoTitle" />
        <activity
            android:name=".activity.SynopsisActivity"
            android:theme="@style/SmartMarketTheme.NoTitle" />
        <activity
            android:name=".activity.TestActivity"
            android:theme="@style/SmartMarketTheme.NoTitle" />
        <activity
            android:name=".activity.FeedbackActivity"
            android:theme="@style/SmartMarketTheme.NoTitle" />
        <activity
            android:name=".activity.FaultDescriptionActivity"
            android:theme="@style/SmartMarketTheme.NoTitle" />
        <activity
            android:name=".activity.FaultetailsActivity"
            android:theme="@style/SmartMarketTheme.NoTitle" />
        <activity
            android:name=".activity.GasDetailsActivity"
            android:theme="@style/SmartMarketTheme.NoTitle" />
        <activity
            android:name=".activity.MeteorologyetailsDActivity"
            android:theme="@style/SmartMarketTheme.NoTitle" />
        <activity
            android:name=".activity.CustomGJSubmitActivity"
            android:theme="@style/SmartMarketTheme.NoTitle" />

        <activity
            android:name=".activity.TemperatureMonitorActivity"
            android:theme="@style/SmartMarketTheme.NoTitle" />
        <activity
            android:name=".activity.FaultListActivity"
            android:theme="@style/SmartMarketTheme.NoTitle" />
        <activity
            android:name=".activity.TemperDetectActivity"
            android:launchMode="singleInstance"
            android:screenOrientation="landscape"
            android:theme="@style/SmartMarketTheme.FullScreen" />
        <activity
            android:name=".activity.GMapViewActivity"
            android:launchMode="standard"
            android:theme="@style/SmartMarketTheme.NoTitle" />
        <activity
            android:name=".activity.AboutUsActivity"
            android:launchMode="standard"
            android:theme="@style/SmartMarketTheme.NoTitle" />
        <!-- 历史告警查询-->
        <activity
            android:name=".activity.HistoryAlarmQueryActivity"
            android:launchMode="standard"
            android:theme="@style/SmartMarketTheme.NoTitle" />


        <activity
            android:name=".activity.VideoPlayActivity"
            android:screenOrientation="landscape"
            android:theme="@style/SmartMarketTheme.FullScreen" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.bohui.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <meta-data
            android:name="com.amap.api.v2.apikey"
            android:value="57e348cf8c6f776e4d34316260afaed9" />
        <service android:name="com.amap.api.location.APSService" />
        <!--宇视SDK配置Activity-->
        <activity
            android:name="io.ionic.starter.activity.Demo3Activity"
            android:screenOrientation="landscape"
            android:theme="@style/SmartMarketTheme.NoTitle"
            android:windowSoftInputMode="adjustUnspecified|stateHidden" />
        <activity
            android:name="io.ionic.starter.activity.Demo1ListActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SmartMarketTheme.NoTitle"
            android:windowSoftInputMode="adjustUnspecified|stateHidden" />
        <activity
            android:name="io.ionic.starter.activity.Demo4ListActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SmartMarketTheme.NoTitle"
            android:windowSoftInputMode="adjustUnspecified|stateHidden" />
        <activity
            android:name="io.ionic.starter.activity.CMainHengMenu"
            android:screenOrientation="landscape"
            android:theme="@style/SmartMarketTheme.NoTitle"
            android:windowSoftInputMode="adjustUnspecified|stateHidden" />
        <activity
            android:name="io.ionic.starter.activity.CMainShuMenu"
            android:screenOrientation="portrait"
            android:theme="@style/SmartMarketTheme.NoTitle"
            android:windowSoftInputMode="adjustUnspecified|stateHidden" />
        <activity
            android:name=".activity.TaskManagerActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SmartMarketTheme.NoTitle"
            android:windowSoftInputMode="adjustUnspecified|stateHidden" />
        <!-- Rich push 核心功能 since 2.0.6-->
        <activity
            android:name="cn.jpush.android.ui.PopWinActivity"
            android:exported="false"
            android:theme="@style/MyDialogStyle"></activity>

        <!-- Required SDK核心功能-->
        <activity
            android:name="cn.jpush.android.ui.PushActivity"
            android:configChanges="orientation|keyboardHidden"
            android:exported="false"
            android:theme="@android:style/Theme.NoTitleBar">
            <intent-filter>
                <action android:name="cn.jpush.android.ui.PushActivity" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="${applicationId}" />
            </intent-filter>
        </activity>

        <!-- Required SDK 核心功能-->
        <!-- 可配置android:process参数将PushService放在其他进程中 -->
        <service
            android:name="cn.jpush.android.service.PushService"
            android:process=":pushcore">
            <intent-filter>
                <action android:name="cn.jpush.android.intent.REGISTER" />
                <action android:name="cn.jpush.android.intent.REPORT" />
                <action android:name="cn.jpush.android.intent.PushService" />
                <action android:name="cn.jpush.android.intent.PUSH_TIME" />
            </intent-filter>
        </service>

        <!-- since 3.0.9 Required SDK 核心功能-->
        <provider
            android:name="cn.jpush.android.service.DataProvider"
            android:authorities="${applicationId}.DataProvider"
            android:exported="false"
            android:process=":pushcore" />

        <!-- since 1.8.0 option 可选项。用于同一设备中不同应用的JPush服务相互拉起的功能。 -->
        <!-- 若不启用该功能可删除该组件，或把 enabled 设置成 false ；App 不会被其他 App 拉起，但会拉起其他的 App。 -->
        <service
            android:name="cn.jpush.android.service.DaemonService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="cn.jpush.android.intent.DaemonService" />
                <category android:name="${applicationId}" />
            </intent-filter>

        </service>

        <!-- 可选，如果使用静态Activity方式拉起，该组件必须声明 -->
        <activity
            android:name="cn.jpush.android.service.DActivity"
            android:enabled="true"
            android:exported="true"
            android:taskAffinity="jpush.custom"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="cn.jpush.android.intent.DActivity" />
                <category android:name="${applicationId}" />
            </intent-filter>
        </activity>

        <!-- since 3.1.0 Required SDK 核心功能-->
        <provider
            android:name="cn.jpush.android.service.DownloadProvider"
            android:authorities="${applicationId}.DownloadProvider"
            android:exported="true" />

        <!-- Required SDK核心功能-->
        <receiver
            android:name="cn.jpush.android.service.PushReceiver"
            android:enabled="true">
            <intent-filter android:priority="1000">
                <action android:name="cn.jpush.android.intent.NOTIFICATION_RECEIVED_PROXY" />   <!--Required  显示通知栏 -->
                <category android:name="${applicationId}" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.USER_PRESENT" />
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
            <!-- Optional -->
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <action android:name="android.intent.action.PACKAGE_REMOVED" />

                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <!-- Required SDK核心功能-->
        <receiver
            android:name="cn.jpush.android.service.AlarmReceiver"
            android:exported="false" />

        <!-- 3.5.0新增，用于定时展示功能 -->
        <receiver
            android:name="cn.jpush.android.service.SchedulerReceiver"
            android:exported="false" />

        <!-- 3.6.7 新增，用于负反馈组件 -->
        <service android:name="cn.jpush.android.service.MessagingIntentService" />

        <!-- User defined.  For test only  用户自定义的广播接收器-->
        <receiver
            android:name=".receiver.MyReceiver"
            android:enabled="true">
            <intent-filter>
                <action android:name="cn.jpush.android.intent.REGISTRATION" /> <!--Required  用户注册SDK的intent-->
                <action android:name="cn.jpush.android.intent.MESSAGE_RECEIVED" /> <!--Required  用户接收SDK消息的intent-->
                <action android:name="cn.jpush.android.intent.NOTIFICATION_RECEIVED" /> <!--Required  用户接收SDK通知栏信息的intent-->
                <action android:name="cn.jpush.android.intent.NOTIFICATION_OPENED" /> <!--Required  用户打开自定义通知栏的intent-->
                <action android:name="cn.jpush.android.intent.CONNECTION" /><!-- 接收网络变化 连接/断开 since 1.6.3 -->
                <category android:name="${applicationId}" />
            </intent-filter>
        </receiver>


        <!-- User defined.  For test only  用户自定义接收消息器,3.0.7开始支持,目前新tag/alias接口设置结果会在该广播接收器对应的方法中回调-->
        <!--since 3.3.0 接收JPush相关事件-->
        <receiver android:name=".receiver.PushMessageReceiver">
            <intent-filter>
                <action android:name="cn.jpush.android.intent.RECEIVE_MESSAGE" />
                <category android:name="${applicationId}"></category>
            </intent-filter>
        </receiver>

        <!--since 3.3.0 Required SDK核心功能-->
        <activity
            android:name="cn.jpush.android.service.JNotifyActivity"
            android:exported="false"
            android:taskAffinity="jpush.custom"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="cn.jpush.android.intent.JNotifyActivity" />
                <category android:name="${applicationId}" />
            </intent-filter>
        </activity>
        <!-- since 3.5.6 新增华硕通道  -->
        <receiver android:name="cn.jpush.android.asus.AsusPushMessageReceiver"></receiver>
        <!-- Required  . Enable it you can get statistics data with channel -->
        <meta-data
            android:name="JPUSH_CHANNEL"
            android:value="${JPUSH_CHANNEL}" />
        <meta-data
            android:name="JPUSH_APPKEY"
            android:value="${JPUSH_APPKEY}" /> <!--  </>值来自开发者平台取得的AppKey-->
    </application>

</manifest>